<%= form_for @traveller, :url => traveller_path(@traveller), :method => :patch do |f| %>
    
    <%= render :partial => 'form', :locals => { :f => f } %>

    <%= f.submit "Update",:class => "btn btn-primary" %>
<% end %>